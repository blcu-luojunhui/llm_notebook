services:
  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    networks:
      - task_pilot_default

networks:
  task_pilot_default:
    external: true
